[project]
name = "ai4research-web"
version = "0.1.0"
description = "智能科研方法课程网站（MkDocs Material）"
requires-python = ">=3.10"
dependencies = [
    "mkdocs>=1.6",
    "mkdocs-material>=9.5",
]

[tool.uv]
package = false
