# 申请原件、外部数据与凭据：不纳入版本管理
.local/
applied/
/data
/raw
/results

# 克隆的外部参考资料库（自带 .git，避免被作为子模块或散文件纳入）
/references/library/research-method/

# references/library/ 下的外部原件（论文/书/演讲/视觉模板），仅由 README 作索引，不入版本管理
/references/library/papers/
/references/library/books/
/references/library/talk/
/references/library/template/

# 迁移过渡：ref/ 旧路径下的同名原件（ref/ → references/ 完成后可删除下列四行）
/ref/papers/
/ref/books/
/ref/talk/
/ref/template/

# 本地模型/API 配置
.env
.env.*
!.env.example

# Office 临时文件与自动备份
~$*
*.bak

# macOS
.DS_Store

# 编辑器
.vscode/
.idea/
*.swp

# 本地渲染、模板审计、解包、layout、QA 缓存与中间产物
.work/
/.tmp/
/.ppt_master_inputs/
node_modules/

# inspection / layout 自动产物
*.ndjson
